# 项目上下文与技术栈
- 语言: C# 12/13, .NET 10
- 框架: Avalonia UI (最新稳定版)
- 架构模式: Clean Architecture (Domain, Application, Infrastructure, UI)
- UI 模式: MVVM (使用 CommunityToolkit.Mvvm)

# 代码规范
- 命名: 类和属性使用 PascalCase，私有字段使用 _camelCase。
- 异步: 所有的 I/O 和硬件通讯操作必须使用 async/await。
- 依赖注入: 必须通过构造函数注入服务，禁止使用静态单例模式。

# UI (Avalonia) 规则
- 严禁在 .axaml.cs 中编写业务逻辑。
- 在 XAML 中必须明确指定 x:DataType 以开启编译时绑定。
- 优先使用 Avalonia 的内部样式系统，避免行内样式定义。

# 架构约束
- UI 层只能引用 Application 层，不能直接引用 Infrastructure 层的具体实现。
- 领域逻辑（Domain）必须保持纯粹，不得引用任何 UI 库或数据库框架。
- 所有的硬件 SDK 调用（如 Sony Camera SDK）必须封装在 Infrastructure 层并实现 Domain 定义的接口。

# 交互偏好
- 在生成代码前，如果架构设计存在歧义，请先询问我。
- 回答请使用中文。